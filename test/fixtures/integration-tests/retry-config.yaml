test-suites:
  buildkite-quarantined-retries-with-config:
    command: bash -c 'exit 123'
    fail-on-upload-error: true
    results:
      language: Ruby
      framework: RSpec
      path: ./fixtures/integration-tests/with-config-buildkite-rspec-failed-not-quarantined.json-UNIQUE-*
    retries:
      attempts: 2
      command: echo "{{ tests }}"
  circleci-quarantined-retries-with-config:
    command: bash -c 'exit 123'
    fail-on-upload-error: true
    results:
      language: Ruby
      framework: RSpec
      path: ./fixtures/integration-tests/with-config-circleci-rspec-failed-not-quarantined.json-UNIQUE-*
    retries:
      attempts: 2
      command: echo "{{ tests }}"
  github-actions-quarantined-retries-with-config:
    command: bash -c 'exit 123'
    fail-on-upload-error: true
    results:
      language: Ruby
      framework: RSpec
      path: ./fixtures/integration-tests/with-config-github-actions-rspec-failed-not-quarantined.json-UNIQUE-*
    retries:
      attempts: 2
      command: echo "{{ tests }}"
  gitlab-ci-quarantined-retries-with-config:
    command: bash -c 'exit 123'
    fail-on-upload-error: true
    results:
      language: Ruby
      framework: RSpec
      path: ./fixtures/integration-tests/with-config-gitlab-ci-rspec-failed-not-quarantined.json-UNIQUE-*
    retries:
      attempts: 2
      command: echo "{{ tests }}"
  generic-quarantined-retries-with-config:
    command: bash -c 'exit 123'
    fail-on-upload-error: true
    results:
      language: Ruby
      framework: RSpec
      path: ./fixtures/integration-tests/with-config-generic-rspec-failed-not-quarantined.json-UNIQUE-*
    retries:
      attempts: 2
      command: echo "{{ tests }}"
  inherited-env-quarantined-retries-with-config:
    command: bash -c 'exit 123'
    fail-on-upload-error: true
    results:
      language: Ruby
      framework: RSpec
      path: ./fixtures/integration-tests/with-config-inherited-env-rspec-failed-not-quarantined.json-UNIQUE-*
    retries:
      attempts: 2
      command: echo "{{ tests }}"
  oss-quarantined-retries-with-config:
    command: bash -c 'exit 123'
    fail-on-upload-error: true
    results:
      language: Ruby
      framework: RSpec
      path: ./fixtures/integration-tests/with-config-oss-rspec-failed-not-quarantined.json-UNIQUE-*
    retries:
      attempts: 2
      command: echo "{{ tests }}"
